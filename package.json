{
  "private": true,
  "name": "echosign",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:server": "pnpm --filter @echosign/server dev",
    "dev:web": "pnpm --filter @echosign/web dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "tsup": "^8",
    "vitest": "^2",
    "vite": "^6",
    "tsx": "^4"
  }
}
